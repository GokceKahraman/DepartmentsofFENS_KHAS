<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.departmentsoffens_khas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DepartmentsofFENS_KHAS">
        <activity android:name=".EnergyDept"></activity>
        <activity android:name=".MechatronicsDept" />
        <activity android:name=".CivilDept" />
        <activity android:name=".IndustrialDept" />
        <activity android:name=".ElectricalDept" />
        <activity android:name=".MolecularDept" />
        <activity android:name=".ComputerDept" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>